# Write a Python program to check if a variable is divisible by another number using the modulo operator.
# Variables
num = 10
divisor = 3

# Check if num is divisible by divisor
if num % divisor == 0:
    print(num, "is divisible by", divisor)
else:
    print(num, "is not divisible by", divisor)
